repos:

- repo: local
  hooks:
    - id: swift-format
      name: swift-format
      language: system
      entry: xcrun swift-format format --in-place --configuration .swift-format --recursive .
      require_serial: true
      types: [swift]
